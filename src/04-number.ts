(() => {
  let productPrice = 100;
  productPrice = 150;

  console.log('productPrice', productPrice);

  let cutomerAge: number = 28;
  // cutomerAge = cutomerAge + '1';
  cutomerAge = cutomerAge + 1;
  console.log('cutomerAge', cutomerAge);

  let productStock: number;
//  console.log('productStock', productStock);

  let discount = parseInt('123'); //tipo number
  console.log('discount', discount);

  let hex = 0xfff;
  console.log('hex', hex);
  let bin = 0b1010;
  console.log('bin', bin)
}) ();
